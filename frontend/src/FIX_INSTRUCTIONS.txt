FIX REQUIRED:

Your frontend currently calls:
${API_BASE}/api/...

Since API_BASE already includes /api, remove the extra /api.

Example:
WRONG: ${API_BASE}/api/chair-workflow
RIGHT: ${API_BASE}/chair-workflow
